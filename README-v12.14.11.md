# v12.14.11 — Curriculum Units Compact Workspace Repair

Targeted UI-only repair for the current Curriculum Units listing.

Goals:
- keep the entire working table visible inside the desktop content area;
- make filters compact on large screens;
- give the Unit name the largest share of horizontal space;
- keep Programme as a short code;
- reduce room preference text from "No preference" to "None";
- retain the existing compact overflow actions;
- avoid global DataTable changes.

No database, Supabase, unit validation, scheduling, or timetable logic is changed.
