# v12.11.3 — Programme Stages UI Theme Alignment

UI-only redesign for the Programme Stages page.

Changes:
- removes the off-theme navy hero;
- uses the application's existing primary teal and neutral design tokens;
- compact enterprise page header;
- smaller metric cards;
- collapsible programme cards;
- collapsible stage sections;
- compact unit checkbox grid;
- teal primary Save Stage Units action;
- responsive desktop/mobile spacing;
- no database, query, route, or business-rule changes.

The stage/unit data and save action remain unchanged.
